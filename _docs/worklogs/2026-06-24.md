# 2026-06-24 작업 일지

## 작업 내용

### 1. 디자인 리뉴얼 — 기반 (브랜치 `feat/design-renewal-deals`)
- **브랜드 컬러 인디고화** (`globals.css`): `:root` OKLCH 토큰 hue를 277(인디고)로 통일. `primary`/`ring`/`accent`/`sidebar-*` 일괄 변경 → 추후 색상 교체가 한 곳(hue 값)에서 가능하도록 단일화. 다크모드 미사용, 라이트/화이트 base 확정.
- **사이드바 다크 테마** (`app-shell.tsx`): `zinc-900` 다크 사이드바 + 인디고 브랜드 마크, lucide 아이콘 내비, 하단 사용자 아바타 + 드롭다운(로그아웃). flex.team 구조 + Pennylane 패턴 참고.

### 2. 거래(Deals) 화면 전면 리뉴얼
- claude design 시안을 실제 shadcn/Next 코드로 포팅.
- 요약 카드 4종(매출/매입/손익/미입금), 칩형 필터 바, 밀도 높은 데이터 테이블(상태 pill·담당자 아바타·카테고리 뱃지), 번호형 페이지네이션.
- **전월 대비(MoM) 실데이터 지표** (옵션 A): `listDeals`에 직전 달 집계 추가. 귀속연월(year+month)이 **둘 다** 지정될 때만 직전 달과 비교(1월→전년 12월), 동일 상품구분·담당자 조건으로 같은 모수끼리 비교. 매출/매입 카드에 ▲/▼ 증감률 표시. 전월 값 0이면 "신규", 월 미지정 시 기존 설명 문구 유지.

### 3. 거래처·판관비 목록 디자인 통일
- 거래처(`counterparties/page.tsx`): 토픽바·검색 칩·패널 카드·테이블 스타일을 거래 화면과 통일(상호명 primary 링크, 종목 accent 뱃지).
- 판관비(`expenses/page.tsx`): 요약 카드(총 지출액·건수) 추가 + 동일 패널/테이블(항목 accent 뱃지, 결제수단 라벨 매핑).

### 4. 등록/수정 폼 화면 디자인 적용
- 거래/거래처의 `new`·`[id]` 4개 페이지 토픽바 통일(px-8, 22px 볼드, 상단 "← 목록" back 링크).
- 폼 본문 패딩 `p-6` → `px-8` 정렬(헤더 가로 라인 일치).
- 거래 폼 매출/매입 섹션 헤더를 이모지(💰📦) → lucide 아이콘(Coins/Package) + accent 박스로 교체, 손익 색상 `text-red-600` → `text-destructive` 토큰화.

### 5. 판관비 필터 바 추가
- `expenses-filters.tsx` 신규: 연도·월·항목·결제수단 칩 필터 + 품목/거래처 검색.
- DB 생성 컬럼(`year`/`month`, `expenseDate` 파생) 기준 필터링, URL 쿼리 파라미터 배선.

### 6. 대시보드 신규 + 사이드바 섹션 구조화
- **대시보드** (`/` 루트, 기존 `/deals` 리다이렉트 대체): `getDashboard` 집계 쿼리 신규. 이번 달 매출/매입/손익(마진율)·연 누적 손익 카드, 미입금/미결산 알림(예정일 지난 건, 클릭 시 해당 필터로 이동), 최근 거래 10건. 기획서 `07_mvp_wireframes` 메인 화면(P1) 반영.
- **사이드바 섹션화**: 메인 / 영업 / 리포트 / 관리 4섹션 그룹 헤더. 미구현(리포트·관리, P2)은 "준비중" 비활성 칩으로 자리만 배치 → 전체 제품 윤곽 가시화.

### 커밋 (논리 단위 10개, `feat/design-renewal-deals`)
`34d2ecf` 인디고 토큰 → `37acd9b` 사이드바 다크 → `5f10587` 거래 리뉴얼+전월대비 → `16fe529` dev 포트 3300 → `a9b7927` 거래처 → `4fb501c` 판관비 → `8091d6d` 폼 화면 → `e30a58c` 판관비 필터 → `9d11bbe` 대시보드 → `87a19e4` 사이드바 섹션화

## 결정 사항
- **라이트/화이트 base 확정, 다크모드 미사용.** 디자인 레퍼런스: flex.team(구조) + Pennylane(시각). 메모 `design-renewal-direction.md` 참조.
- **브랜드 컬러 인디고(hue 277), 검정 사이드바 + 인디고 조합.** 색상은 OKLCH 토큰 단일화로 추후 hue 값만 바꾸면 전체 교체 가능.
- **전월 대비는 귀속연월 둘 다 지정될 때만 노출**(모호성 회피), 미지정 시 정적 설명 문구.
- **사이드바는 전체 메뉴 구조를 미리 노출**하되 미구현(P2 리포트·관리)은 "준비중" 비활성 처리 → 빈 화면 없이 제품 윤곽 확보.
- 현재 메뉴 3개(거래·거래처·판관비)는 P0 핵심 + P1 판관비 범위였고, 기획상 대시보드·리포트·관리가 더 있음을 재확인.

## 이슈 & 해결
- **디자인 시안과 포팅 결과 미세 불일치** → claude design 파일 재확인(최신 맞음), 포팅 중 실용적 치환 3건(번호형 페이지네이션, export 버튼, 카드 아이콘 border 박스) 시안대로 복구.
- **dev 포트 혼선(3000 vs 3300)** → `.env.local`의 `AUTH_URL`은 이미 3300. `package.json` dev 스크립트를 `next dev -p 3300`으로 정합. 다른 세션이 3300 점유 중이라 백그라운드 `npm run dev`는 EADDRINUSE(정상).
- **flex.team 제품 이미지 로그인 월** → raw HTML curl + asset URL grep으로 실제 제품 카드 이미지 추출, Pennylane 공개 스크린샷으로 시각 레퍼런스 보강.
- **대시보드 쿼리 반환 객체 `month` 키 중복** → `period`/`thisMonth`로 구조 분리.
- 미입금/미결산 알림은 `*DueDate < today AND status pending`만 집계 → 시드 데이터 예정일이 미래/공란이면 0건 표시될 수 있음(데이터 의존, 정상 동작).

## 다음 할 일
- [ ] 3300에서 전 화면 최종 시각 검증(대시보드 → 거래 → 거래처 → 판관비 → 각 폼)
- [ ] `feat/design-renewal-deals` → `main` 머지
- [ ] export 버튼(거래 필터 Download) 실제 CSV 내보내기 기능 배선 (현재 미연결)
- [ ] Pretendard 폰트 실제 적용 검토(현재 후보)
- [ ] (P1~P2) 리포트 모듈 — 매출장표·월별손익·TOP거래처·십일조
- [ ] (P2) 관리 모듈 — 사용자·Lookup 마스터·마이그레이션 리뷰 큐

---

# 2026-06-24 작업 일지 (오후 — P0~P2 기능구현, 브랜치 `feat/p0-p2-features`)

## 작업 내용
- **권한 기반 모듈** (`src/server/auth/guards.ts`): 역할/행단위 권한 술어 (스펙 06_permissions 기준) — Deal/Counterparty/Expense/admin 가드 + `getSessionUser`.
- **P0**
  - 거래 상태별 수정 권한: 영업은 본인·draft만 금액변경, closed는 회계 전용, 편집 폼 읽기전용 배너 + fieldset 비활성화. `createDeal` 영업 담당 강제.
  - 거래처 역할 필터(매체사/광고주/대행사) + 사업자번호 없는 곳만 토글 + 역할 배지. 민감필드(사업자번호/계좌/수수료) 회계+admin 전용.
  - 내 프로필(`/profile`) + 이름·비밀번호 변경(`updateProfile`, 현재 비밀번호 검증), 사용자 메뉴 링크.
- **P1**
  - 판관비 다행 입력 폼(`/expenses/new`) + 일괄저장(`createExpenses`, 영업 payer 본인 강제) + 목록 입력 버튼.
  - 매출장표(`/reports/ledger`) — 귀속월/통장(현금) 기준 토글 월별 피벗.
  - CSV export(거래/거래처/판관비) — UTF-8 BOM + 인젝션/이스케이프 방지, 필터 반영 서버 액션 + 다운로드 버튼.
- **P2**
  - 월별손익+십일조(`/reports/pnl`), TOP 거래처(`/reports/top-counterparties`, 매출/손익순 토글).
  - admin 사용자 관리(역할·활성·신규 등록) + Lookup 마스터 편집(`/admin/*`), 전 페이지 진입 가드.
  - 사이드바 리포트 메뉴 활성화 + admin 섹션 역할 조건부 노출("준비중" 칩 제거).
- **검증**: tsc 0 errors, `npm run build` 20개 라우트 성공. dev 서버(3300) 인증 세션 라이브 점검 — 신규 라우트 전부 200 + 콘텐츠 렌더, sales 계정 `/admin` 307 차단, 역할별 사이드바 노출 확인.
- **커밋(논리 단위 9개)**: `baf21b6` 권한헬퍼 → `682b56e` 거래권한 → `dd7197b` 거래처필터 → `3813ecd` 프로필 → `16e181c` 판관비입력 → `b86f82f` 리포트3종 → `3149f7e` admin → `3818064` 네비배선 → `59fabd3` CSV.

## 결정 사항
- 디자인 리뉴얼과 성격 분리 위해 전용 브랜치 `feat/p0-p2-features` 신설.
- 독립 신규 기능(리포트/admin/판관비입력/CSV)은 **병렬 서브에이전트**로 구축, 공유 파일(네비/목록/레이아웃) 배선만 직접 통합 → 충돌 0.
- 권한은 우선 **앱 레이어 가드**로 구현, DB RLS는 후속(이중 방어).
- 거래처 "자동생성만" 필터는 식별 마커 컬럼 부재로 **보류**(추측 추가 지양).
- 인센티브 요율 *변경 이력* 테이블은 마이그레이션 필요 → **보류**, 현재는 Lookup 요율 직접 수정.

## 이슈 & 해결
- CSV 거래 export가 `listDeals` 200건 상한에 묶임 → export용 pageSize 상한 5000으로 확대(UI는 50 유지).
- 점검 첫 측정 시 `/expenses` 500 → Turbopack 최초 컴파일 중 일시 오류, 재요청 시 200 안정 확인.
- eslint 잔여 6건은 모두 기존 파일(`deal-form` VAT effect, `counterparty-combobox`)의 사전 경고 — 이번 작업 무관.

## 다음 할 일
- [ ] 서버측 mutation 가드(closed 거래/민감필드 거부) UI 클릭 또는 단위 테스트로 검증
- [ ] accountant 역할 시드 계정 추가(권한 매트릭스 전체 검증용 — 현재 sales/admin만 존재)
- [ ] DB RLS 정책(행 단위 보안) — 앱 레이어와 이중 방어
- [ ] 거래처 머지/정비 UI, 26년 plan 달성률 트래커, audit_log
- [ ] 거래 목록 다중 정렬·상태 필터, 키보드 단축키 등 폼 UX 보강
- [ ] `feat/p0-p2-features` 리뷰 후 머지 + 프로덕션 데이터 임포트(`import:all`) + UAT
