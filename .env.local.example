# Database
DATABASE_URL=postgresql://askim:askim@localhost:5432/askim_erp

# Auth.js — `openssl rand -base64 32`로 생성한 값으로 교체
AUTH_SECRET=replace-me-with-random-string
AUTH_URL=http://localhost:3000

# 운영 환경 (development | production)
NODE_ENV=development
